export type TService = {
  id: number;
  title: string;
  desc: string;
  image: string;
};
export type TSkill = {
  id: number;
  title: string;
  image: string;
};
export type TEducation = {
  year: string;
  institute: string;
  topic: string;
};
